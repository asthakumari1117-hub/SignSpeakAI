# assets/

Place static files here:

- `logo.png` — App logo (displayed in sidebar/header)
- `sign_reference/*.png` — Reference images for each sign (A-Z)
- `gesture_reference/*.png` — Reference images for gestures

These are optional. The app works without them.
